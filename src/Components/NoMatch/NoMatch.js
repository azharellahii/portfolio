import React from "react";
import Nav from "../Nav/Nav";
import './NoMatch.css'
const NoMatch = () => {
    return (
        <div>
            <Nav />
            <span>No Match Found.Try using the Navigation bar to go through different links.</span>
        </div>

    )
}

export default NoMatch